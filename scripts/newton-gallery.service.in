[Unit]
Description=Newton Example Studio (Docker Compose)
Documentation=file://__LAUNCHER_DIR__/README.md
Requires=docker.service
After=docker.service network-online.target
Wants=network-online.target

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=__LAUNCHER_DIR__
Environment=COMPOSE_PROJECT_NAME=newton-gallery
# Pull is intentionally omitted: the Launchable builds the local image once.
ExecStart=/usr/bin/docker compose up -d --remove-orphans
ExecStop=/usr/bin/docker compose stop
TimeoutStartSec=0
TimeoutStopSec=120

[Install]
WantedBy=multi-user.target
